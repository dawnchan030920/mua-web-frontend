import React from "react";
import styled from "styled-components";

type ArticleListPageProps = {
  category: string;
}

const ArticleListPage: React.FC<ArticleListPageProps> = (props) => {
  return (
    <div>

    </div>
  )
}

export default ArticleListPage;